---
pageable: true
---

<!-- Print title showing day/month/year -->
<h1>Archive for
  <% case page_type
     when 'day' %>
    <%= Date.new(year, month, day).strftime("%B %e, %Y") %>
  <% when 'month' %>
    <%= Date.new(year, month, 1).strftime('%B %Y') %>
  <% when 'year' %>
    <%= year %>
  <% end %>
</h1>

<%= partial "partials/summaries.erb", locals: {
    articles: page_articles } %>
